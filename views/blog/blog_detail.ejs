<!DOCTYPE html>
<html>
    <%- include("../partials/head.ejs") %>
    <body>
        <%- include("../partials/header.ejs") %>
        <main>
            <section class="detail-content">
           <article>
            <h1><%= blog.title %></h1>
            <small>Created at: <%= new Date(blog.createdAt).toLocaleDateString() %> at <%= new Date(blog.createdAt).toLocaleTimeString() %></small>
            <p><%= blog.body %></p>

            <% if (blog.category) { %> 
            <p> <strong>Category: </strong> <a href="/category/<%= blog.category._id %>/posts"> <%= blog.category.title %></a></p>
           <% }%>
            <button class="delete" data-doc="<%= blog._id %>">Delete</button>
            <a href="/blog/update/<%= blog._id %>" class="update"  %>">Update</a>
           </article>
        </section>
        </main>
        <%- include("../partials/footer.ejs") %>
        <script src="/main.js" async defer>

        </script>
    </body>
</html>
